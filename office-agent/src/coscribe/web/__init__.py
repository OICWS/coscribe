"""Local Web UI: a FastAPI process serving the chat WebSocket protocol and
the static frontend, bridging Runner's synchronous tool-approval callback
to a WebSocket wait (see session.py's ChatSession)."""
